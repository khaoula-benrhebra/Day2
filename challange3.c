#include<stdio.h>
#include<stdlib.h>
int main ()
{
    int i,N,T[N];
    printf("veuillez entrer la taille de votre tableau");
    scanf("%d",N);
    printf("veuilez saisir les elements de votre tableau");
    for(i=0;i<N;i++)
    {
        printf("T[%d]= ",i);
        scanf("%d",&T[N]);
    }
}